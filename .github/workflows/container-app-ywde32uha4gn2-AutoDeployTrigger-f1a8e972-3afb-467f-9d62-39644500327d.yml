name: Trigger auto deployment for container-app-ywde32uha4gn2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-ywde32uha4gn2-AutoDeployTrigger-f1a8e972-3afb-467f-9d62-39644500327d.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPYWDE32UHA4GN2_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPYWDE32UHA4GN2_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPYWDE32UHA4GN2_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPYWDE32UHA4GN2_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPYWDE32UHA4GN2_REGISTRY_PASSWORD }}
          containerAppName: container-app-ywde32uha4gn2
          resourceGroup: todonodejspgsqlgoogleauth85fd9e99
          imageToBuild: default/container-app-ywde32uha4gn2:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


